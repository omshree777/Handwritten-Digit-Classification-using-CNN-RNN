# Handwritten Digit Classification (CNN & RNN)

This project implements CNN and RNN models using PyTorch to classify handwritten digits from the MNIST dataset.

## Models
- Convolutional Neural Network (CNN)
- Recurrent Neural Network (RNN)

## Dataset
MNIST (28x28 grayscale digit images)

## How to Run

pip install -r requirements.txt
python train.py