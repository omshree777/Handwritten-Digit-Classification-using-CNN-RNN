# Handwritten-Digit-Classification-using-CNN-RNN
Handwritten Digit Classification using CNN and RNN on the MNIST dataset. The project trains and compares both models to recognize digits (0–9) from 28×28 grayscale images. Performance is evaluated using accuracy and confusion matrices to analyze how different architectures learn image data.
